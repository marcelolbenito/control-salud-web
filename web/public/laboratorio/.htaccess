# Mismas reglas que Laboratorio/public (rutas amigables bajo /laboratorio/)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /laboratorio/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
